#include<bits/stdc++.h>
using namespace std;
priority_queue<int,vector<int>,less<int>> que;
int main()
{
    int n,temp,ans=0;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &temp);
        que.push(temp);
        
    }
    
}