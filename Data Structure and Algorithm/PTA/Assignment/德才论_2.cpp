#include<cstdio>
#include<algorithm>
#include<iostream>
using namespace std;
typedef struct score
{
    int count;
    int values[110];
}Score;
Score sce[10010];
bool cmp(Score a,Score b) {    
    
}
int main()
{
    int m,n;
    scanf("%d %d", &m, &n);
    sort(sce, sce+m, cmp);
    for (int i = 0; i < m; i++)
    {
        
    }
    return 0;
}